/* Rate = double. Since double can provide speed advantages over a float, and rate is a decimal number.
 * Principal = long long. Sometimes the principal can be very large, exceeding int size.
 * Payment = int. An integer should suffice.
*/